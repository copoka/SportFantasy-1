<%= form_for(@amplua) do |f| %>
  <% if @amplua.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amplua.errors.count, "error") %> prohibited this amplua from being saved:</h2>

      <ul>
      <% @amplua.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
