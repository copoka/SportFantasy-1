<%= form_for(@user_team_player) do |f| %>
  <% if @user_team_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_team_player.errors.count, "error") %> prohibited this user_team_player from being saved:</h2>

      <ul>
      <% @user_team_player.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
