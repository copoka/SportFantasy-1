<%= form_for(@real_team) do |f| %>
  <% if @real_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@real_team.errors.count, "error") %> prohibited this real_team from being saved:</h2>

      <ul>
      <% @real_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
